# TASK-0002: Obsidian Vaultアダプタ

## タスク概要

- **タスクタイプ**: DIRECT
- **推定工数**: 3h
- **依存タスク**: なし

## 実装ファイル

- `storage-adapters/obsidian-vault.md`

## 実装詳細

### 変数契約（27変数）

backlog-wikiと同一インターフェース。`template-assembly.md` の「カテゴリ3: obsidian-vault選択時の値」に定義された27変数を全て提供する。

必須変数: `storage_name`（`Obsidian Vault`）, `storage_create_cmd`（`write_note`）, `storage_read_cmd`（`read_note`）, `storage_update_cmd`（`patch_note` or `write_note(mode: overwrite)`）, `storage_search_cmd`（`search_notes(query: ...)`）, `storage_session_init`（2-Phase手順）, 他22変数

### 2-Phase初期化（最適化A）

旧方式（直列N回）を2フェーズに短縮:
- **Phase 1**（直列・必須）: `list_directory(path: "{{storage_base_path}}")` でVault構成確認
- **Phase 2**（並列）: `read_note` でHome・各設定ノート・INDEXを一括読み込み・パース・キャッシュ

### ディレクトリ構造

```
{base_path}/
├── deals/          ← 案件コンテキスト
├── knowledge/      ← ナレッジベース
├── logs/           ← 活動ログ（{YYYY-MM}/{YYYY-MM-DD}.md）
├── settings/       ← 設定（slack-channels.md等）
├── guidelines/     ← ガイドライン
└── HOME.md         ← ホームINDEX
```

### ノート操作

| 操作 | MCPツール | 備考 |
|------|---------|------|
| 作成 | `write_note(path, content, frontmatter)` | frontmatterでメタデータ管理 |
| 読み込み | `read_note(path)` | content + frontmatter返却 |
| 部分更新 | `patch_note(path, oldString, newString)` | 差分のみ置換 |
| 全体更新 | `write_note(path, content, mode: "overwrite")` | |
| Frontmatter更新 | `update_frontmatter(path, frontmatter, merge: true)` | |
| 検索 | `search_notes(query, limit)` | クエリにパスプレフィクス含め範囲絞り込み可 |
| 一覧 | `list_directory(path)` | ファイル・ディレクトリリスト |

### Frontmatter構造

```yaml
---
format_version: 1
created: "YYYY-MM-DDTHH:MM:SS"
updated: "YYYY-MM-DDTHH:MM:SS"
tags:
  - context
  - {product_prefix}
category: "{カテゴリ名}"
customer: "{顧客名}"
---
```

### 書き込み確認プロトコル

backlog-wikiと同一プロトコル。全書き込み操作前にユーザー承認が必要。

## テスト実装状況

- [ ] 単体テスト
- [ ] 統合テスト
- [ ] E2Eテスト
