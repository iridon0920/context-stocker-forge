---
name: {{product_prefix}}-deal
description: >
  {{product_name}}案件コンテキストの保存・復元スキル。{{storage_description}}でチーム共有の
  案件状況（顧客→案件の階層構造）を管理する。Slack・Backlog課題・Google Calendar・
  Google Drive・Gmailからの補足情報はユーザーの要求時にオンデマンドで取得する
  （デイリーログ時のみ全データソースを自動参照）。
  INDEXページ管理、活動レポート生成、鮮度管理、セットアップ確認にも対応。
version: 5.0.0
---

# {{product_name}}案件コンテキスト管理

## {{storage_name}}情報

| 項目 | 値 |
|------|-----|
| プロジェクトキー | `{{project_key}}` |
| ストレージ | {{storage_name}} |
| 顧客ページ | `コンテキスト/{既存顧客\|新規顧客}/{顧客名}` |
| 案件ページ | `コンテキスト/{既存顧客\|新規顧客}/{顧客名}/{案件名}` |
| ナレッジ | `ナレッジ/{カテゴリ}/{トピック名}` |
| 日次ログ | `活動ログ/YYYY-MM/YYYY-MM-DD` |
| 週次レポート | `活動ログ/YYYY-MM/週次レポート-W{NN}` |
| 月次レポート | `活動ログ/YYYY-MM/月次レポート` |
| 年次レポート | `活動ログ/YYYY/年次レポート` |
| 設定 | `設定/{設定名}` |
| Homeページ | `Home` — 各INDEXへのリンク集 |
| INDEXページ | `INDEX/{カテゴリ名}` — {{index_count}}つの個別INDEX |
| 共有範囲 | チーム共有（{{team_scope}}） |

### コンテキスト階層構造

コンテキストは **顧客ページ** と **案件ページ** の2階層で管理する。

- **顧客ページ**: 基本情報、連絡先、案件一覧
- **案件ページ**: 概要、{{sales_framework}}、As-Is/To-Be、意思決定ログ、次のアクション 等

詳細は `references/context-format.md` を参照。

### INDEX構成

| ページ | 内容 |
|--------|------|
| `Home` | 各INDEXへのリンク集・エントリ数サマリー |
| `INDEX/顧客・案件` | 既存/新規顧客の案件一覧 |
{{#index_pages}}
| `INDEX/{{index_name}}` | {{index_description}} |
{{/index_pages}}

詳細は `references/index-format.md` を参照。

{{storage_mcp_tool_table}}

## セッション初期化（重要）

スキルを使う際、まず以下の情報を取得してセッションキャッシュに保持する。
**⚡ パフォーマンス最適化: Phase内の複数MCP呼び出しは必ず並列実行すること。**

{{storage_session_init}}

---

## セッションキャッシュルール（重要）

以下の情報は **初回取得後、セッション終了まで再取得しない**。これによりMCP呼び出し回数を大幅に削減する。

| キャッシュ項目 | 取得タイミング | 無効化条件（再取得が必要になるケース） |
|-------------|-------------|----------------------------------|
| プロジェクトID（またはVaultパス） | Phase 1 | なし（不変） |
| HomeページのwikiId + 本文 | Phase 2-3 | `/{{product_prefix}}-admin index` 実行時のみ |
| 各INDEXページのwikiId + 本文 | 初回参照時 | INDEX更新操作を自分で行った直後 |
| Slackチャンネル設定（パース済み） | Phase 2-3 | `/{{product_prefix}}-admin slack` 実行時のみ |
| Backlogプロジェクト設定（パース済み） | Phase 2-3 | `/{{product_prefix}}-admin backlog` 実行時のみ |
| 競合情報設定（パース済み） | Phase 2-3 | `/{{product_prefix}}-admin competitors` 実行時のみ |
| 料金体系設定（パース済み） | Phase 2-3 | `/{{product_prefix}}-admin pricing` 実行時のみ |
| チームメンバー設定（パース済み） | Phase 2-3 | `/{{product_prefix}}-admin members` 実行時のみ |
| 顧客・案件のwikiIdマッピング | INDEX読み込み時に自動構築 | コンテキスト保存でエントリ追加した時に追加更新 |

### キャッシュ運用ルール

1. **取得済み判定**: 同一セッション内で既に取得した情報は、無効化条件に該当しない限り再取得しない
2. **無効化時の挙動**: 上記テーブルの無効化条件に該当する操作を行った場合のみ、該当項目を次回アクセス時に再取得する
3. **wikiIdマッピング活用**: INDEXから構築した「ページ名→wikiId」のマッピングを保持し、{{storage_search_cmd}} による検索ステップを省略して直接 {{storage_read_cmd}} で本文取得する

> **効果**: コンテキスト復元→作業→保存のフロー等で、INDEXや設定の重複読み込みを完全に排除。

---

## データソースアクセス方針

本スキルが使用するデータソースと、そのアクセスポリシーを以下に定義する。

### デフォルトデータソース（常時使用）

| データソース | 用途 |
|-------------|------|
| {{storage_name}} (`{{project_key}}`) | コンテキスト・ナレッジ・INDEX・設定・活動ログの読み書き |

**{{storage_name}}はこのプラグインの唯一のストレージであり、すべてのコマンドでデフォルトで使用する。**

### オンデマンドデータソース（ユーザー要求時のみ）

| データソース | 利用可能な情報 |
|-------------|--------------|
{{#data_sources}}
| {{source_name}} | {{source_description}} |
{{/data_sources}}

**これらのデータソースは自動的に読み出さない。** コンテキスト復元・保存・レポート生成時に、ユーザーに対して「以下の外部データソースからも補足情報を取得できますが、必要ですか？」と提案し、ユーザーが求めた場合のみ取得する。

### 外部データソース取得時のフィルタリング方針

Google Calendar・Gmailなどの外部データソースから情報を取得する際は、**{{product_name}}事業に関連する情報のみを対象とする**。

- **Google Calendar**: 予定のタイトル・参加者・説明文から{{product_name}}関連（顧客名、製品名、案件名など）と判断できるもののみ抽出する。明らかに無関係な予定（他事業の定例、全社イベント等）は除外する
- **Gmail**: 検索クエリに{{product_name}}関連キーワード（製品名、顧客名等）を含め、関連メールのみを対象とする
- **Google Drive**: {{product_name}}関連の議事録・ドキュメントのみを対象とする
- **判断に迷う場合のみ**、ユーザーに「この予定/メールは{{product_name}}関連として取り込みますか？」と確認を取る。明確に関連する/しないと判断できる場合は確認不要

### 例外：デイリーログ（/{{product_prefix}}-log daily）

デイリーログ作成時のみ、接続可能な全データソースを自動参照する。これは日次の活動記録を網羅的に収集する目的であり、このコマンドに限り外部データソースへの自動アクセスを行う。ただし、取得した情報のうち{{product_name}}事業に関連するもののみをログに記録する（上記フィルタリング方針に従う）。

---

## 書き込み確認プロトコル

**すべてのデータ書き込み前に必ず以下の手順を実行する。**

### 新規作成の場合
1. 書き込む内容の全文を表示する
2. 「この内容で保存してよいですか？」と確認する
3. 承認を得てから {{storage_create_cmd}} を実行する
4. 該当INDEXページを更新する

### 更新の場合
1. 既存の内容と新しい内容の **差分** を表示する
2. 差分の表示形式:
```
■ 変更点:
  [セクション名]
  - 旧: 〇〇〇
  + 新: △△△

  [セクション名]（新規追加）
  + 新しい内容

  [セクション名]（削除）
  - 削除される内容
```
3. 「この変更内容で保存してよいですか？」と確認する
4. 承認を得てから {{storage_update_cmd}} を実行する
5. 必要に応じてINDEXページも更新する

---

## Slackチャンネル設定の読み取り

Slack検索を行う際は、まず {{storage_search_cmd}} で `設定/Slackチャンネル設定` のページを検索し、重点チャンネル一覧を読み取る。
見つからない場合は旧名でも検索する。
{{#default_slack_channels}}
どちらもない場合はデフォルトチャンネル（{{default_channels_list}}）を使用する。
{{/default_slack_channels}}

---

## Backlogプロジェクト設定の読み取り

Backlog課題やWiki更新のチェック対象を決定するため、{{storage_search_cmd}} で `設定/Backlogプロジェクト設定` のページを検索し、監視プロジェクト一覧を読み取る。
{{#default_backlog_projects}}
見つからない場合はデフォルト（{{default_projects_list}}）を使用する。
{{/default_backlog_projects}}

設定のフォーマットは `references/backlog-projects-format.md` を参照。

---

## 競合情報設定管理

`/{{product_prefix}}-admin competitors` コマンドで呼び出される。競合サービスの情報を設定ページとして管理する。

### 設定の取得

{{storage_search_cmd}} で `設定/競合情報` を検索。
- 見つかった場合: {{storage_read_cmd}} で内容を取得し、テーブルをパース
- 見つからない場合: デフォルト値を案内し、設定ページの作成を提案

### 設定の表示

現在の競合サービス一覧をテーブル形式で表示する。

### 設定の編集

1. ユーザーの指示に従い、テーブルの行を追加・編集・削除
   - 新しい競合を追加する場合はサービス名・ベンダー・ポジショニング・差別化ポイントを記入
   - 比較の観点も必要に応じて追加・変更可能
2. **書き込み確認プロトコル** に従い差分を表示して確認を取る
3. {{storage_update_cmd}} でページ内容を更新

フォーマットは `references/competitors-format.md` を参照。

---

## 料金体系設定管理

`/{{product_prefix}}-admin pricing` コマンドで呼び出される。料金体系の情報を設定ページとして管理する。

### 設定の取得

{{storage_search_cmd}} で `設定/料金体系` を検索。
- 見つかった場合: {{storage_read_cmd}} で内容を取得
- 見つからない場合: デフォルト値を案内し、設定ページの作成を提案

### 設定の表示

現在の料金体系（概要・プラン表・注意事項）を表示する。

### 設定の編集

1. ユーザーの指示に従い、概要テキスト、プラン・課金体系テーブル、見積作成時の注意事項を編集
   - プラン追加・廃止、価格改定時に更新
   - ディスカウント条件の変更も反映
2. **書き込み確認プロトコル** に従い差分を表示して確認を取る
3. {{storage_update_cmd}} でページ内容を更新

フォーマットは `references/pricing-format.md` を参照。

---

{{^excluded_admin_members}}
## チームメンバー設定管理

`/{{product_prefix}}-admin members` コマンドで呼び出される。チームメンバーの情報を設定ページとして管理する。

### 設定の取得

{{storage_search_cmd}} で `設定/チームメンバー` を検索。
- 見つかった場合: {{storage_read_cmd}} で内容を取得
- 見つからない場合: 設定ページの新規作成を提案

### 設定の表示

現在のメンバー一覧（名前・役割・スキルタグ・Slack・連絡先・備考）を表形式で表示する。

### メンバーの追加（add）

1. ユーザーに以下をヒアリング:
   - **名前**（必須）
   - **役割**（必須。例: 営業, SE, PM, CS）
   - **スキルタグ**（任意。カンマ区切り。例: API連携, 製造業, ネゴシエーション）
   - **Slack**（任意。@メンションID）
   - **連絡先**（任意。メールアドレス等）
   - **備考**（任意）
2. メンバー一覧テーブルに行を追加
3. **書き込み確認プロトコル** に従い差分を表示して確認を取る
4. {{storage_update_cmd}} でページ内容を更新

### メンバーの削除（remove [名前]）

1. 指定された名前でメンバー一覧テーブルを検索
2. 該当メンバーの情報を表示して削除確認を取る
3. テーブルから該当行を削除
4. **書き込み確認プロトコル** に従い差分を表示して確認を取る
5. {{storage_update_cmd}} でページ内容を更新

### メンバー情報の編集

1. ユーザーの指示に従い、指定メンバーのフィールドを更新
2. **書き込み確認プロトコル** に従い差分を表示して確認を取る
3. {{storage_update_cmd}} でページ内容を更新

フォーマットは `references/team-members-format.md` を参照。

---

## チームメンバー候補提案ロジック

チームメンバー設定が存在する場合、各機能でメンバー候補の提案を行う。**設定が存在しない場合は本セクションの処理をすべてスキップする（graceful degradation）。**

### マッチングルール（優先順）

1. **役割マッチ**: タスクタイプから必要な役割を推定して絞り込み
   - 技術的タスク（API、設計、PoC等）→ SE
   - 交渉・折衝タスク（契約、価格、条件等）→ 営業
   - 管理・調整タスク（進捗、スケジュール等）→ PM
   - 運用・サポートタスク → CS
2. **スキルタグマッチ**: 案件のキーワード（業種、技術要素、製品分野等）とメンバーのスキルタグの一致度
3. **既存アサイン考慮**: その案件の「社内担当」テーブルに既に記載されているメンバーを優先（継続性）

### 提案フォーマット

提案は常に `💡` マーク付きの advisory として表示する。強制しない。

```
💡 担当候補:
  - {メンバー名}（{役割}）— {マッチ理由}
  - {メンバー名}（{役割}）— {マッチ理由}
```

### 提案が行われる場面

| 場面 | トリガー | 提案内容 |
|------|---------|---------|
| コンテキスト保存 | 新規案件作成時、または社内担当テーブルが空 | 社内担当候補（営業+SE を基本構成として提案） |
| コンテキスト復元 | 担当が未記載の次のアクションがある場合 | アクション内容に適したメンバー候補 |
| ドキュメント生成 | ドキュメント生成完了後 | レビュアー候補（種別→役割マッピング: 提案書→営業+PM、見積→営業+PM、技術文書→SE） |

---

{{/excluded_admin_members}}
## INDEXページ管理

### INDEXの構成

INDEXは **Homeページ** と **{{index_count}}つの個別INDEXページ** で構成する。
Homeはリンク集のみ。個別INDEXが実際のエントリ一覧を持つ。

フォーマットは `references/index-format.md` を参照。

### INDEX更新のタイミング

以下の操作後にINDEXページを更新する：
- コンテキスト（顧客ページ or 案件ページ）の新規作成 → `INDEX/顧客・案件` + Home
- ナレッジの新規作成 → 該当カテゴリのINDEX + Home
- エントリ名の変更 → 該当INDEXページ
- INDEXに記載のメタ情報（ステータス、最終更新日等）が変わる場合 → 該当INDEXページ
- 顧客ステータスの変更（新規→既存） → `INDEX/顧客・案件`

{{storage_index_update_procedure}}

### INDEX再構築（/{{product_prefix}}-admin index）

{{storage_index_rebuild_procedure}}

詳細手順は `references/index-format.md` の「INDEX再構築時」セクションを参照。

要約：
1. 全ページを取得しパスプレフィックスで分類
2. 顧客/案件の階層判定（パスの深さで区別）
3. 旧命名規約の検出とマイグレーション提案
4. 各ページの最終更新日を取得
5. {{index_count}}つの個別INDEXページ + Homeページを再構築
6. **書き込み確認プロトコル** に従い確認を取ってから保存

---

## コンテキスト復元

案件のコンテキストから復元する手順。

### 1. コンテキストの読み込み

#### 1-1. INDEXからの検索

`INDEX/顧客・案件` の内容から、指定された顧客名に該当するエントリを探す。
INDEXには{{#customer_classifications}}「{{classification_name}}」{{/customer_classifications}}のセクションがあるため、すべて確認する。

見つかった場合：
- **⚡ wikiIdがINDEXに記載されていれば、{{storage_read_cmd}} で直接ページを取得**（{{storage_search_cmd}} での検索ステップをスキップ）
- wikiIdが未記載の場合のみ {{storage_search_cmd}} でページ名検索し、取得後にwikiIdをINDEXに補完記録する
- 顧客ページを取得し、特定の案件が指定されていればその案件ページも取得
- 指定がなければ顧客ページの案件一覧から進行中の案件を中心に取得
- **複数の案件ページを取得する場合は並列で {{storage_read_cmd}} を呼び出す**

見つからない場合：
- {{storage_search_cmd}} で `コンテキスト/` + 顧客名でも検索（INDEX未登録の可能性に対応）
- 旧命名規約でも検索（マイグレーション前のページに対応）
- どちらでもヒットしない場合は「この顧客のコンテキストはまだありません。新規作成しますか？」と案内
- 旧命名規約のページが見つかった場合は、読み込みつつマイグレーション（ページ名変更）を提案

#### 1-2. 類似案件の横断表示

コンテキスト復元時に、関連する他の案件情報も補足する：
- INDEXの案件一覧から同じ{{product_name}}プラン、同じ製品を使っている案件を抽出
- 過去に同様の課題を解決した案件があれば、その知見を提示
- {{storage_search_cmd}} で関連キーワード（製品名等）を検索し、関連ナレッジも提示

#### 1-3. 外部データソースの提案（オンデマンド）

ストレージ読み込み後、ユーザーに以下の外部データソースからの補足取得を提案する：

```
以下の外部データソースからも補足情報を取得できます。必要なものがあれば教えてください：
{{#data_sources}}
- {{source_name}}: {{source_hint}}
{{/data_sources}}
```

ユーザーが求めた場合のみ、該当するデータソースを取得する。

### 2. 状況サマリーの表示

読み込んだ情報を以下の形式で簡潔に表示する：

```
[顧客名] {{product_name}}コンテキスト復元

■ 分類: 既存顧客 / 新規顧客
■ アクティブ案件:
  - [案件名A]: ステータス（概要を一行で）
  - [案件名B]: ステータス（概要を一行で）
■ 次のアクション:
  - アクション1（案件A）— 担当: {名前}
  - アクション2（案件B）— [未定]
■ 未決事項: （あれば）
■ 関連リンク:
  - [#Slackチャンネル](URL)
  - [Backlog課題](URL)
■ 注意事項: （懸念・リスクがあれば）
■ 補足情報: （外部データソースを取得した場合のみ表示）
■ 類似案件: （同プラン・同製品の案件があれば簡潔に）
```

{{^excluded_admin_members}}
担当が「[未定]」の次のアクションがある場合、「チームメンバー候補提案ロジック」に従い適任メンバーを `💡` 提案する。

{{/excluded_admin_members}}
### 3. 作業開始の確認

「何から始めますか？」と確認し、作業に入る。

---

## コンテキスト保存

セッションで扱った案件の現在状況を保存する手順。

### 1. セッション内容の整理

今回のセッションで議論・作業した内容から以下を抽出する：

- **現在のステータス**: 案件がどの段階にあるか
- **今回やったこと**: 具体的な作業内容、調査結果
- **決定事項**: 確定した方針や判断（判断理由・検討した代替案も含む）。出典リンクが特定できれば付与
- **未決事項**: まだ決まっていないこと、検討中のこと
- **次のアクション**: 次にやるべき具体的なタスク（担当者がわかれば記載）
- **懸念・リスク**: 注意が必要なこと
- **{{sales_framework}}情報の更新**: セッション中に得られた情報
- **関連リンク**: セッション中に参照したSlackメッセージ、Backlog課題、ドキュメントのURL

### 2. 補足情報の収集（オンデマンド）

保存前に、ユーザーに外部データソースからの補足取得を提案する：

```
保存前に以下の外部データソースから補足情報を取得できます。必要なものがあれば教えてください：
- Google Calendar: 直近の打合せ予定を関連リンクに追加
- Google Drive: 議事録フォルダから関連ドキュメントを検索
- Gmail: 関連メールの情報を反映
```

ユーザーが求めた場合のみ、該当するデータソースを取得する（**フィルタリング方針に従い、{{product_name}}事業に関連する情報のみを対象とする**）：
- **Google Calendar**: `gcal_list_events` で顧客に関連する直近の打合せ予定を検索し、{{product_name}}関連の予定のみを関連リンクセクションに追加
{{#drive_folder_id}}
- **Google Drive**: `google_drive_search` で議事録フォルダ（ID: `{{drive_folder_id}}`）内の関連ドキュメントを検索し、関連リンクに追加
{{/drive_folder_id}}
{{^drive_folder_id}}
- **Google Drive**: `google_drive_search` で関連ドキュメントを検索し、関連リンクに追加
{{/drive_folder_id}}
- **Gmail**: `gmail_search_messages` で顧客名や{{product_name}}関連のキーワードで検索し、重要なメールの情報をコンテキストに反映

### 3. ストレージへの保存

#### 3-1. 既存ページの確認

`INDEX/顧客・案件` および {{storage_search_cmd}} で `コンテキスト/` + 顧客名を検索し、既存の顧客ページ・案件ページがあるか確認する。
旧命名規約でも検索する。

#### 3-2. 顧客分類の確認（新規作成時）

新規作成の場合、顧客が「既存顧客」か「新規顧客」かを確認する：
- 過去に受注実績（製品・サービス問わず）があるか
- 不明な場合はユーザーに確認する

#### 3-3. 顧客ページと案件ページの準備

**コンテキストは顧客ページ + 案件ページの2階層で保存する。**

##### 顧客ページ
- 既存の顧客ページがあればマージ（案件一覧に新案件を追加等）
- なければ `references/context-format.md` の顧客ページフォーマットで新規作成

##### 案件ページ
- 既存の案件ページがあれば `references/context-format.md` のマージルールに従ってマージ
- なければ案件ページフォーマットで新規作成
- **未収集情報フラグ**: {{sales_framework}}各項目の充実度を自動チェックし、未収集の場合は「次のアクション」に⚠️付きの情報収集タスクを追加
{{^excluded_admin_members}}
- **社内担当の提案**: 新規案件作成時、または顧客ページの「社内担当」テーブルが空の場合、「チームメンバー候補提案ロジック」に従い担当候補を `💡` 提案する。ユーザーが採用した場合は社内担当テーブルに反映する
{{/excluded_admin_members}}

#### 3-4. 類似エントリチェック（新規作成時のみ）

新規の顧客ページまたは案件ページの場合、`references/similarity-check.md` のプロトコルに従い既存ページと照合する。
- 顧客ページ: クロスカテゴリ類似（既存/新規で同名がないか）
- 案件ページ: 階層間の整合性チェック（親の顧客ページが存在するか）

既存ページの上書き更新ではこのステップをスキップする。

#### 3-5. 書き込み確認（必須）

**書き込み確認プロトコル** に従い、ページの内容を表示して確認を取る。

- **新規の場合**: ページ全文を表示
- **更新の場合**: 差分を表示
- 顧客ページと案件ページが両方更新される場合は、まとめて差分を表示

#### 3-6. 保存実行

{{storage_save_context_procedure}}

---

## 活動レポート生成

`/{{product_prefix}}-log report` コマンドで呼び出される。INDEXとストレージのデータを集約してレポートを生成する。

### 引数

| 引数 | 説明 | 例 |
|------|------|----|
| `monthly [YYYY-MM]` | 月次報告用レポート | `/{{product_prefix}}-log report monthly 2026-02` |
| `qbr [YYYY-Q#]` | QBR用レポート | `/{{product_prefix}}-log report qbr 2026-Q3` |
| `annual [YYYY]` | 年次レポート | `/{{product_prefix}}-log report annual 2025` |
| `--format md` | Markdown出力（ストレージ保存用。デフォルト） | |
| `--format pptx` | PowerPoint出力 | |
| `--format both` | Markdown + PowerPoint両方出力 | |

※ 日付未指定時は前月（monthly）/ 前四半期（qbr）/ 前年度（annual）をデフォルトとする。

### データソース

レポート生成に使用するデータソースとその取得方法：

| データソース | 取得方法 | 用途 |
|------------|---------|------|
| KPI管理ページ | {{storage_read_cmd}} で `管理/KPI/YYYY` を取得 | 売上目標・実績・達成率 |
| OKR管理ページ | {{storage_read_cmd}} で `管理/OKR/YYYY` を取得 | OKR進捗・月次アクション |
| 週次レポート | {{storage_search_cmd}} で `活動ログ/YYYY-MM/週次レポート-W*` を検索 | 月次レポートの活動データ集約元 |
| コンテキスト（案件ページ） | INDEX/顧客・案件 → 各案件ページを {{storage_read_cmd}} で取得 | 案件ステータス・売上規模・パイプライン |
| INDEXページ群 | {{storage_read_cmd}} で各INDEXを取得 | エントリ数・更新状況 |

**⚡ 階層型集約**: 月次レポートは週次レポートを読む。週次レポートが存在しない月は日次ログにフォールバック（警告表示）。QBRは月次レポートを、年次はQBRレポートを読む。

### 月次レポートの構成（10セクション）

以下の構成で月次レポートを生成する。

#### セクション1: 表紙

```markdown
# {{product_name}}チーム 月次報告

**FY{YY} {M}月（YYYY年MM月）**

{{organization_name}}
{{team_name}}
```

#### セクション2: エグゼクティブサマリ

対象月の活動を3〜5文で要約。ハイライト・課題・次月の方向性を含む。

```markdown
## エグゼクティブサマリ

{対象月の主要な成果、課題、方向性を3〜5文で記述}
```

生成ロジック:
1. KPIの目標vs実績から達成状況を把握
2. 新規商談・クローズ商談の件数を集計
3. OKRの進捗ステータスを確認
4. 上記を踏まえて要約文を生成

#### セクション3: 月次主要KPI

KPI管理ページ（`管理/KPI/YYYY`）から対象月のデータを取得。

```markdown
## 月次主要KPI

| 項目 | 目標 | 実績 | 達成率 | 前年実績 | 昨対比 |
|------|------|------|--------|---------|-------|
| 売上合計 | {目標} | {実績} | {実績÷目標×100}% | {前年} | {実績÷前年×100}% |
{{#kpi_revenue_categories}}
| └ {{.}} | {目標} | {実績} | {達成率}% | {前年} | {昨対比}% |
{{/kpi_revenue_categories}}
| 粗利率 | {目標} | {実績} | — | {前年} | — |
| 新規商談数 | {目標} | {実績} | {達成率}% | {前年} | {昨対比}% |
```

生成ロジック:
1. `管理/KPI/YYYY` の「月次目標」行から対象月の目標値を取得
2. 「月次実績」行から対象月の実績値を取得
3. 「前年実績」行から前年同月の値を取得
4. 達成率 = 実績 ÷ 目標 × 100、昨対比 = 実績 ÷ 前年実績 × 100 を算出

#### セクション4: 商談パイプライン（直近3ヶ月）

KPI管理ページの実績値をもとに直近3ヶ月の推移を表示。

```markdown
## 商談パイプライン（直近3ヶ月）

| 月 | 新規商談 | クローズ（受注） | クローズ（失注） | 成約率 | パイプライン残 |
|----|---------|----------------|----------------|-------|-------------|
| {M-2}月 | N件 | N件 | N件 | XX% | N件 |
| {M-1}月 | N件 | N件 | N件 | XX% | N件 |
| {M}月 | N件 | N件 | N件 | XX% | N件 |
```

生成ロジック:
1. KPI管理ページの「月次実績」から直近3ヶ月の商談数・受注/失注数を取得
2. KPIに値がない場合は、コンテキストページ群から集計:
   - 新規商談: 対象月に作成された `コンテキスト/{{customer_classification_from}}/` 配下の案件ページ数
   - 受注: ステータスが「完了」になった案件数
   - 失注: ステータスが「失注」になった案件数
3. 成約率 = 受注 ÷ (受注+失注) × 100
4. パイプライン残 = ステータスが「商談中」「進行中」の案件数

#### セクション5: 新規商談一覧

対象月に開始した商談の一覧。

```markdown
## 新規商談一覧

| 顧客名 | 案件名 | 案件種別 | 予算規模 | ステータス | 見込み時期 |
|--------|--------|---------|---------|-----------|----------|
| {顧客名} | [{案件名}](ストレージリンク) | {案件種別} | {Budget} | 商談中 | {Timeline} |
```

生成ロジック:
1. `INDEX/顧客・案件` から対象月に更新された{{customer_classification_from}}の案件を抽出
2. 各案件ページの概要セクション（案件種別）と{{sales_framework}}（Budget, Timeline相当）を取得

#### セクション6: エンジニア稼働状況

週次レポートのティア3テーブルからエンジニアの稼働状況を集計。

```markdown
## エンジニア稼働状況

| 案件 | 稼働日数 | 主な作業内容 | 備考 |
|------|---------|------------|------|
| {顧客名}/{案件名} | N日 | {作業サマリ} | {備考} |
| ナレッジ整備 | N日 | {作業サマリ} | |
| その他（社内） | N日 | {作業サマリ} | |
```

生成ロジック:
1. 対象月の週次レポート（`活動ログ/YYYY-MM/週次レポート-W*`）を全取得
2. 各週次レポートの「ティア3: 数値集約」→「案件活動サマリー」テーブルから案件ごとの活動日数を月間合計
3. 「ナレッジ活動」テーブルの件数を集計
4. 「ミーティング」テーブルから社内MTG回数・時間を「その他」として集計
{{^excluded_admin_members}}
5. チームメンバー設定が存在する場合、メンバー別の活動サマリーも生成:

```markdown
### メンバー別活動サマリー

| メンバー | 役割 | 担当案件数 | 活動日数 | 主な貢献 |
|---------|------|-----------|---------|---------|
| {名前} | {役割} | N件 | N日 | {主な成果サマリ} |
```

データソース: 週次レポートのメンバー別活動テーブル + コンテキストページの社内担当テーブル
{{/excluded_admin_members}}

#### セクション7: アクション実施結果

OKR管理ページの月次アクション実績とコンテキストの意思決定ログから生成。

```markdown
## 今月のアクション結果

| # | アクション | 目的 | 結果 | 次のステップ |
|---|----------|------|------|------------|
| 1 | {アクション内容} | {目的} | {結果} | {次のステップ} |
```

生成ロジック:
1. `管理/OKR/YYYY` の対象月の「月次アクション実績」行を全OKRから収集
2. 日次活動ログの案件活動から主要なアクションを補完
3. コンテキストページの意思決定ログ（CM側）から対象月の決定事項を抽出

#### セクション8: OKR進捗

OKR管理ページのKRテーブルをそのまま引用。

```markdown
## OKR進捗

### OKR1: {Objective}

| KR# | Key Result | 達成基準 | 現在値 | 進捗率 | ステータス |
|-----|-----------|---------|--------|-------|-----------|
| 1-1 | {KR} | {基準} | {現在値} | XX% | 順調/注意/遅延 |
| ... | ... | ... | ... | ... | ... |

### OKR2: {Objective}
（同様のテーブル）

### OKR3: {Objective}
（同様のテーブル）
```

生成ロジック:
1. `管理/OKR/YYYY` からOKR定義とKRテーブルをそのまま取得・引用

#### セクション9: FY通期 主要KPIまとめ

KPI管理ページの累計データから通期見通しを生成。

```markdown
## FY通期 主要KPIまとめ

| 項目 | 年間目標 | 通期累計 | 進捗率 | 着地見込み |
|------|---------|---------|-------|----------|
| 売上合計 | {年間目標} | {通期累計} | {進捗率}% | {着地見込み} |
{{#kpi_revenue_categories}}
| └ {{.}} | {年間目標} | {通期累計} | {進捗率}% | {着地見込み} |
{{/kpi_revenue_categories}}
| 粗利率 | {目標} | {通期平均} | — | — |
| 新規商談数 | {年間目標} | {通期累計} | {進捗率}% | {着地見込み} |
```

生成ロジック:
1. `管理/KPI/YYYY` の「月次目標」から年間目標を取得
2. 「月次実績」の通期累計列を取得
3. 進捗率 = 通期累計 ÷ 年間目標 × 100
4. 着地見込み = 通期累計 ÷ 経過月数 × 12（線形推計）

#### セクション10: 注意事項・次月の方針

```markdown
## 注意事項・次月の方針

### 注意が必要な案件
- {顧客名}/{案件名} — {理由（30日以上更新なし / 懸念・リスクあり 等）}

### 次月の重点アクション
- {アクション内容}（関連: OKR X-X / KPI項目）
```

生成ロジック:
1. 鮮度管理ロジック（後述）で「危険」判定の案件を抽出
2. コンテキストページの「懸念・リスク」「次のアクション」から未対応項目を集計
3. OKRのステータスが「注意」「遅延」のKRに紐づくアクションを提案

### レポート出力先

| 形式 | 出力先 | 説明 |
|------|-------|------|
| Markdown | ストレージ `活動ログ/YYYY-MM/月次レポート` | ストレージ保存用。{{storage_create_cmd}} で保存 |
| pptx | ローカルファイル出力 | PowerPoint形式。pptxスキルを使用して10スライド構成で生成 |

### pptx出力時のスライド構成

PowerPoint出力時は、上記10セクションをそれぞれ1スライドとして構成する（計10スライド）。
pptxスキル（`/pptx`）のSKILL.mdに従い、python-pptxを使用して生成する。

| スライド# | タイトル | 内容 |
|----------|---------|------|
| 1 | 表紙 | タイトル、年月、会社名・部署名 |
| 2 | エグゼクティブサマリ | 要約テキスト |
| 3 | 月次主要KPI | KPIテーブル（目標・実績・達成率・昨対比） |
| 4 | 商談パイプライン | 直近3ヶ月のパイプラインテーブル |
| 5 | 新規商談一覧 | 新規商談テーブル |
| 6 | エンジニア稼働状況 | 稼働テーブル |
| 7 | アクション結果 | アクション実績テーブル |
| 8 | OKR進捗 | OKRごとのKRテーブル |
| 9 | FY通期KPIまとめ | 通期KPIテーブル + 着地見込み |
| 10 | 注意事項・次月方針 | 注意案件リスト + 重点アクション |

### QBRレポートの生成

QBRレポートは**3つの月次レポート**を集約し、以下を追加する（日次ログ・週次レポートは直接読まない）。

1. **四半期KPIサマリ**: 3ヶ月分のKPIを集約、四半期目標との達成率
2. **商談トレンド分析**: 商談パイプラインの3ヶ月推移とトレンド
3. **OKR四半期レビュー**: 期初からの進捗推移、ステータス変遷
4. **ナレッジカバレッジ分析**: カテゴリ別の充実度評価

**データソース**: 対象四半期の3つの月次レポート（`活動ログ/YYYY-MM/月次レポート`）を読み込む。

**フォールバック**: 月次レポートが存在しない月は、週次レポートにフォールバック。週次も存在しない場合は日次ログにフォールバック（警告表示）。

```markdown
## 四半期KPIサマリ（YYYY Q#）

| 項目 | Q目標 | Q実績 | Q達成率 | 前Q実績 | QoQ |
|------|------|------|--------|--------|-----|
| 売上合計 | {Q目標} | {Q実績} | XX% | {前Q} | XX% |
| ... | ... | ... | ... | ... | ... |

## 四半期トレンド

| 月 | 売上 | 新規商談 | 受注 | 失注 | 成約率 |
|----|------|---------|------|------|-------|
| {M-2}月 | {金額} | N件 | N件 | N件 | XX% |
| {M-1}月 | {金額} | N件 | N件 | N件 | XX% |
| {M}月 | {金額} | N件 | N件 | N件 | XX% |

## ナレッジカバレッジ

| カテゴリ | エントリ数 | 直近Q追加 | カバレッジ評価 |
|---------|-----------|---------|-------------|
{{#knowledge_categories}}
| {{category_name}} | N件 | +N件 | 充実/標準/不足 |
{{/knowledge_categories}}

## 改善提案

{データに基づく具体的な改善提案を3〜5項目で生成}
```

### 年次レポートの生成

年次レポートは**4つのQBRレポート**を集約する（月次・週次・日次は直接読まない）。

**データソース**: 対象年度の4つのQBRレポート（`活動ログ/YYYY/QBR-Q*`）+ KPI管理ページ + OKR管理ページ（最新値は直接取得）。

**フォールバック**: QBRが存在しない四半期は月次レポートにフォールバック。月次も存在しない場合はさらに下位にフォールバック（警告表示）。

構成:
1. **表紙**: タイトル、年度、会社名・部署名
2. **年間エグゼクティブサマリ**: 4四半期の要約を年間視点でまとめ
3. **年間KPI実績**: KPI管理ページの年間目標vs実績、達成率、前年度比較
4. **商談トレンド（年間）**: 四半期ごとの推移グラフ用データ（売上・新規商談・受注・失注・成約率）
5. **主要ディールレビュー**: QBRのティア1イベントを年間集約（受注・失注・重大障害・契約）
6. **OKR年間レビュー**: 年度OKRの最終達成状況
7. **ナレッジ蓄積状況**: カテゴリ別のエントリ数推移
8. **次年度への提言**: データに基づく改善提案（5〜10項目）

出力先: `活動ログ/YYYY/年次レポート`

### レポート生成の制約事項

- **データソース**: ストレージ内のデータのみ使用。外部データソースへの直接アクセスは行わない
- **階層型集約**: 各レポートは一段下のレポートのみを読む（月次←週次、QBR←月次、年次←QBR）。日次ログを直接読むのは週次レポートのみ
- **フォールバック**: 下位レポートが未生成の場合、さらに一段下にフォールバックする。フォールバック時は `⚠️` 警告を表示し、下位レポートの生成を促す
- **KPIデータ未入力時**: KPI管理ページに実績値がない（`—`）場合、該当項目を「未入力」と表示し、`/{{product_prefix}}-admin kpi-set actual` での入力を促す
- **日次ログ不足時**: 日次ログがない日の活動はINDEXの更新日から推定。推定した項目は「※推定」マークを付与
- **書き込み確認**: Markdown形式でストレージ保存する場合は、保存前にレポート内容を表示して確認を取る

---

## 鮮度管理

`/{{product_prefix}}-admin stale` コマンドで呼び出される。更新が滞っているコンテキスト・ナレッジを検出する。

### 判定基準

| 種別 | 警告（黄） | 危険（赤） |
|------|-----------|-----------|
| 案件コンテキスト（アクティブ） | 14日以上更新なし | 30日以上更新なし |
{{#stale_thresholds}}
| {{category_label}} | {{warn_days}}日以上更新なし | {{danger_days}}日以上更新なし |
{{/stale_thresholds}}

### 実行手順

1. `INDEX/顧客・案件` および各ナレッジINDEXから全エントリの最終更新日を確認
2. INDEXに更新日がない場合は {{storage_get_updated_date_cmd}} で各ページの最終更新日を確認
3. 判定基準に基づいて分類
4. 以下のフォーマットで出力：

```
{{product_name}}鮮度チェック（YYYY-MM-DD）

🔴 要対応（危険）:
  - [顧客名A] / [案件名] — 最終更新: 45日前{{^excluded_admin_members}} — 担当: {名前}（{役割}）{{/excluded_admin_members}}
  - ナレッジ/パートナー/規定名 — 最終更新: 100日前

🟡 確認推奨（警告）:
  - [顧客名B] / [案件名] — 最終更新: 20日前{{^excluded_admin_members}} — 担当: {名前}（{役割}）{{/excluded_admin_members}}

✅ 最近更新済み: N件
```

---

## 日次活動ログ

`/{{product_prefix}}-log daily` コマンドで呼び出される。当日のチーム活動を記録する。

### 目的

- 月次・QBRレポートの元データを日次で蓄積し、レポート生成時の負荷を削減する
- 案件の進捗を日単位でトラッキング可能にする

### 実行手順

データ収集の詳細手順は `references/daily-log-format.md` の「データ収集手順」セクションを参照。

#### 1. データ収集

以下の情報を自動収集する（詳細手順は `references/daily-log-format.md` に準拠）。
**⚡ パフォーマンス最適化: Phase内の複数MCP呼び出しは必ず並列実行すること。**

##### Phase 1（前提データ取得 — セッションキャッシュから取得可能）
セッション初期化で既にキャッシュ済みの場合はスキップ。未取得の場合のみ以下を **並列で** 取得：
- `設定/Backlogプロジェクト設定` のページから監視プロジェクト一覧{{#default_backlog_projects}}（見つからない場合はデフォルト値: {{default_projects_list}}）{{/default_backlog_projects}}
- `設定/Slackチャンネル設定` のページからチャンネル一覧
- `INDEX/顧客・案件` から顧客名一覧（Gmail検索キーワード用）

##### Phase 2（全データソースを並列取得）
Phase 1の設定情報を使い、以下を **すべて同時に** MCP呼び出しする：

| # | データソース | MCP呼び出し | 備考 |
|---|-----------|-----------|------|
| 2-1 | Google Calendar | `gcal_list_events`（当日JST 00:00-23:59、`condenseEventDetails: false`） | {{product_name}}関連予定のみ抽出 |
| 2-2 | Slack | `slack_search_public`（バッチクエリ: `(in:#ch1 OR in:#ch2 OR ...) after:{timestamp}`） | 全チャンネル一括検索。6件以上は5件ずつ分割して並列 |
| 2-3 | Gmail | `gmail_search_messages`（顧客名・製品名をORで結合） | 対象事業関連のみ |
| 2-4 | Backlog課題 | 各プロジェクトの `get_issues`（`updatedSince: "YYYY-MM-DD"`） | 複数プロジェクト並列 |
| 2-5 | Google Drive | `google_drive_search`（当日更新の議事録検索） | {{#drive_folder_id}}フォルダID: `{{drive_folder_id}}`{{/drive_folder_id}} |
| 2-6 | ストレージ更新 | {{storage_daily_log_wiki_check}} | — |

##### Phase 3（後続処理 — Phase 2の結果に依存）
Phase 2の結果を使って以下を実行：
- **Gemini文字起こし取得**: Phase 2-1のカレンダー予定に `attachments` があれば `google_drive_fetch` で取得（複数ある場合は並列）。{{product_name}}関連の議論ポイントを要約してカレンダーメモ・案件活動テーブルに反映（大きい場合はTaskサブエージェント）
- **Drive議事録の内容取得**: Phase 2-5で見つかったドキュメントの内容を `google_drive_fetch` で取得（並列可）

#### 2. ドラフト表示・確認

収集した情報を `references/daily-log-format.md` のフォーマットに従ってドラフト表示する。

- 各顧客名・案件名は既存のコンテキストページへのリンクを付与
- 各ナレッジ名は既存のナレッジページへのリンクを付与
- リンク先ページの存在はINDEXから確認し、存在しないページへのリンクは作らない
{{^excluded_admin_members}}
- チームメンバー設定が存在する場合、メンバー別活動サマリーセクションを生成する（Calendar参加者・Slack投稿者・Backlog課題担当をメンバー設定のSlack列で紐づけ）
{{/excluded_admin_members}}

ユーザーに以下を確認する：
- 自動収集できなかった活動（口頭でのやりとり、手動作業等）の追記
- メモセクションへの翌日引き継ぎ事項
- 内容の修正・削除

#### 3. 保存

**書き込み確認プロトコル** に従い保存する。

- ページ名: `活動ログ/YYYY-MM/YYYY-MM-DD`
- 同日のログが既に存在する場合は {{storage_update_cmd}} で上書き更新
- 存在しない場合は {{storage_create_cmd}} で新規作成

#### 4. コンテキストページへの自動反映

日次ログ保存後、収集したGemini文字起こし・議事録・活動情報を関連するコンテキストページに自動反映する（`references/daily-log-format.md` のPhase 5に準拠）：

1. **対象特定**: 日次ログの案件活動テーブルとカレンダーセクションから、関連する顧客・案件のコンテキストページを特定
2. **既存読み込み**: {{storage_read_cmd}} で対象コンテキストページの現在の内容を取得
3. **差分判定**: Gemini文字起こしや議事録の内容が既に反映済みか判定（日付・会議名で照合し、反映済みならスキップ）
4. **更新生成**: 未反映の内容について、経緯・タイムラインへの打合せ記録追記、ステータス更新、次回アクション追記などの更新内容を生成
5. **ユーザー確認**: 各コンテキストページへの更新差分を表示し、**書き込み確認プロトコル** に従いユーザーの承認を得てから {{storage_update_cmd}} で保存
6. **新規ページ**: 対象のコンテキストページが存在しない場合は `/{{product_prefix}}-deal-save` による新規作成を提案

加えて、以下も引き続き提案する：
- 新しいナレッジが生まれた → `/{{product_prefix}}-knowledge-save` を提案

### スケジュール実行

このコマンドは手動実行に加え、`schedule` スキルで定期実行も可能。

推奨設定:
- **実行タイミング**: 平日 17:30（業務終了前）
- **Cronスケジュール**: `30 17 * * 1-5`

スケジュール実行時も、ドラフト表示→ユーザー確認→保存の流れは変わらない（完全自動保存はしない）。

---

## 週次レポート生成

`/{{product_prefix}}-log weekly` コマンドで呼び出される。対象週の日次活動ログを集約し、重要度ティアに基づいて週次レポートを生成する。

### 引数

| 引数 | 説明 | デフォルト |
|------|------|-----------|
| なし | 先週の週次レポートを生成 | 先週（前週の月曜〜日曜） |
| `YYYY-Www` | 指定ISO週の週次レポートを生成（例: `2026-W10`） | — |
| `current` | 今週（途中経過）の週次レポートを生成 | — |

### データソース

| データソース | 取得方法 | 用途 |
|------------|---------|------|
| 日次活動ログ | {{storage_search_cmd}} で `活動ログ/YYYY-MM/YYYY-MM-DD` を対象週の日付範囲で検索 | 集約元データ |

### 生成手順

1. **対象期間の特定**: 引数からISO週番号を決定し、月曜〜日曜の日付範囲を算出
2. **日次ログの取得**: {{storage_search_cmd}} で対象期間の日次ログを検索（月をまたぐ場合は2回の検索を**並列実行**）。見つかったページを {{storage_read_cmd}} で**並列取得**
3. **重要度ティアの判定**: `references/weekly-report-format.md` のティア分類ルールに従い、各エントリの重要度を判定
   - 日次ログの `重要度` 列に値がある場合はその値を優先
   - 空の場合はキーワードマッチで自動判定（★★★ > ★★ > ★）
4. **ティア別集約**:
   - **ティア1（★★★）**: 日次ログエントリの原文をそのまま転記（日付・顧客・案件・詳細・リンク完全保持）
   - **ティア2（★★）**: 案件ステータス変更テーブル + 箇条書き要約
   - **ティア3（★）**: 顧客/案件別の活動日数、ミーティング回数/時間、ナレッジ件数、コミュニケーション件数、Backlog進捗に数値集約
5. **ドラフト表示・確認**: `references/weekly-report-format.md` のフォーマットに従ってドラフトを表示し、ティア分類の修正・追記・メモ記入を確認
6. **保存**: **書き込み確認プロトコル** に従い、`活動ログ/YYYY-MM/週次レポート-W{NN}` に保存。既存の場合は {{storage_update_cmd}}、新規の場合は {{storage_create_cmd}}

### スケジュール実行

推奨設定:
- **実行タイミング**: 毎週金曜 18:00
- **Cronスケジュール**: `0 18 * * 5`

スケジュール実行時もドラフト表示→ユーザー確認→保存のフローを維持する。

詳細フォーマットは `references/weekly-report-format.md` を参照。

---

## Backlogプロジェクト設定管理

`/{{product_prefix}}-admin backlog` コマンドで呼び出される。日次ログやコンテキスト復元で参照するBacklogプロジェクトの設定を管理する。

### 設定の取得

{{storage_search_cmd}} で `設定/Backlogプロジェクト設定` を検索。
- 見つかった場合: {{storage_read_cmd}} で内容を取得し、テーブルをパース
- 見つからない場合: デフォルト値を案内し、設定ページの作成を提案

### 設定の表示（show）

現在の監視プロジェクト一覧をテーブル形式で表示する。

### プロジェクトの追加（add）

1. `get_project` で指定されたプロジェクトキーのプロジェクト情報を取得（存在確認・プロジェクトID取得）
2. テーブルに新しい行を追加
3. **書き込み確認プロトコル** に従い差分を表示して確認を取る
4. {{storage_update_cmd}} でページ内容を更新

### プロジェクトの削除（remove）

1. 指定されたプロジェクトキーがテーブルに存在するか確認
2. 該当行を削除
3. **書き込み確認プロトコル** に従い差分を表示して確認を取る
4. {{storage_update_cmd}} でページ内容を更新

フォーマットは `references/backlog-projects-format.md` を参照。

---

## セットアップ確認

`/{{product_prefix}}-admin setup` コマンドで呼び出される。プロジェクト設定を確認・表示する。

{{storage_setup_procedure}}

---

## フォーマットマイグレーション

`/{{product_prefix}}-admin migrate` コマンドで呼び出される。ストレージ内の既存ページを最新フォーマットに移行する。

### 目的

テンプレートフォーマットの変更（フィールド追加・構造変更・命名規約変更など）が発生した場合に、既存のストレージデータを新しいフォーマットに変換する。

### 対象ページの種類

| 種類 | パスパターン | フォーマット定義 |
|------|------------|----------------|
| 顧客ページ | `コンテキスト/{顧客名}` | `references/context-format.md` |
| 案件ページ | `コンテキスト/{顧客名}/{案件名}` | `references/context-format.md` |
| INDEXページ | `INDEX/*` | `references/index-format.md` |
| 日次活動ログ | `ログ/{YYYY-MM}` | `references/daily-log-format.md` |
| KPI管理 | `KPI管理 FY{YY}` | `references/kpi-format.md` |
| ナレッジ | `ナレッジ/{カテゴリ}/{トピック}` | knowledgeスキル参照 |

### 実行手順

1. **サンプリング確認**
   - ストレージ内のページを最大10件取得
   - 各ページのメタデータ（`format_version`、`最終更新`）を確認
   - ページ種類ごとの件数を集計して表示

2. **フォーマット差分の分析**
   - 現在のページ構造と `references/` 内の最新フォーマット定義を比較
   - 差分の種類を分類:
     - **フィールド追加**: 新しいセクションやメタデータの追加
     - **構造変更**: セクション順序の変更、階層の変更
     - **命名規約変更**: パスやページ名のパターン変更（旧命名規約→新命名規約）
     - **値フォーマット変更**: 日付形式、数値形式などの変更
   - 変更の影響を受けるページ数を種類別に表示

3. **マイグレーション計画の提示**
   - 変更内容と影響範囲をユーザーに表示:
     ```
     === マイグレーション計画 ===
     対象: {N}件のページ

     変更1: {変更内容} → {N1}件に影響
     変更2: {変更内容} → {N2}件に影響
     ...

     実行方式を選択してください:
     (a) 全件一括
     (b) ページ種類別（顧客→案件→INDEX→...の順）
     (c) Nページずつバッチ実行
     ```

4. **ユーザー確認**
   - **書き込み確認プロトコル** に従い、必ず確認を取ってから実行
   - バッチ実行の場合は各バッチの開始前にも確認

5. **マイグレーション実行**
   - 各ページに対して:
     1. {{storage_read_cmd}} でページを読み込み
     2. フォーマット定義に基づいて変換を適用
     3. `format_version` がある場合は最新バージョンに更新
     4. {{storage_write_cmd}} で保存
   - 変換ルール:
     - 新フィールドの追加: デフォルト値または `（未設定）` で初期化
     - 命名規約変更: ページ名のリネーム（{{storage_rename_cmd}} が利用可能な場合）
     - 構造変更: セクション内容を保持したまま並び替え
     - 削除されたフィールド: そのまま残す（データロスを防ぐ）

6. **結果レポート**
   ```
   === マイグレーション結果 ===
   成功: {N1}件
   スキップ（変更不要）: {N2}件
   失敗: {N3}件

   [失敗があれば詳細を表示]
   ```

### 安全対策

- **ドライラン**: 実行前にサンプル1件で変換結果をプレビュー表示し、期待通りか確認する
- **ロールバック情報**: 各ページの変換前内容をログに記録（ただしストレージには保存しない）
- **中断対応**: バッチ実行中にエラーが発生した場合、処理済み件数を表示して中断し、ユーザーに継続/中止を確認
- **冪等性**: 既にマイグレーション済みのページは自動スキップ（`format_version` やフォーマット構造で判定）

---

## 非構造化ファイルのMarkdown化・保存

PDF等の非構造化ファイル（PDF、画像、テキストファイルなど）が提供された場合、内容をMarkdown化してストレージの適切なページに保存する。

### 対象ファイル形式

| 形式 | 処理方法 |
|------|----------|
| PDF | テキスト抽出 → Markdown整形 |
| 画像（PNG/JPG等） | OCR/読み取り → Markdown整形 |
| テキストファイル（.txt, .csv等） | そのままMarkdown整形 |
| Word/Excel/PowerPoint | テキスト抽出 → Markdown整形 |

### 保存先の判断ルール

ファイル内容を分析し、以下のルールで保存先を決定する：

| 内容の種別 | 保存先 |
|-----------|--------|
| 案件に紐づく資料（議事録、要件定義、設計書等） | `コンテキスト/{分類}/{顧客名}/{案件名}` の関連リンクに追加、内容を該当セクションに反映 |
{{#knowledge_categories}}
| {{category_save_hint}} | `ナレッジ/{{category_path}}/{トピック名}` として保存 |
{{/knowledge_categories}}
| ガイドライン文書 | `ガイドライン/{ガイドライン名}` として保存 |
| 判断がつかない場合 | ユーザーに保存先を確認する |

### 処理手順

1. **ファイル読み取り**: ファイルの内容をテキストとして抽出する
2. **内容分析**: 何に関する資料かを判断し、保存先カテゴリを特定する
3. **Markdown整形**: 以下のルールで変換する
   - 見出し構造を `#` / `##` / `###` で再現
   - 表データは Markdownテーブルに変換
   - 箇条書きはリスト形式に変換
   - 画像内のテキストは可能な限りテキスト化
   - 原文の構造・意味を保持しつつ、読みやすい形に整形
4. **保存先の確認**: 判断した保存先をユーザーに提示し確認を取る
5. **既存ページの確認**: 保存先に既存ページがあればマージ、なければ新規作成
6. **書き込み確認プロトコル**: 通常通り確認を取ってから保存
7. **INDEX更新**: 必要に応じて該当INDEXページを更新

### 注意事項

- 大量のデータ（100ページ超のPDF等）の場合は、要約版と全文版のどちらで保存するかユーザーに確認する
- 機密性の高い情報（契約書の金額詳細、個人情報等）が含まれる場合はユーザーに確認してからストレージに保存する
- 元ファイルの出典（ファイル名、取得日時、提供元）をページ末尾に記録する
{{#migration_warnings}}
- {{warning_text}}
{{/migration_warnings}}

---

## ガイドライン参照による業務ドキュメント生成

### 概要

ストレージの `ガイドライン/` 配下に保存されたテンプレート・ガイドライン文書を参照し、案件コンテキストと組み合わせて各種業務ドキュメントを生成する仕組み。

### ガイドラインページの構成

ガイドラインは以下のパスに保存される：

```
ガイドライン/{ドキュメント種別名}
```

例:
- `ガイドライン/商談準備`
- `ガイドライン/提案書作成`
- `ガイドライン/見積書作成`
- `ガイドライン/要件定義用ヒアリングリスト`
- `ガイドライン/設定値`
- `ガイドライン/テストケース`

### ガイドライン参照フロー

1. **ガイドラインページの検索**: {{storage_search_cmd}} で `ガイドライン/{種別名}` を検索
2. **存在確認**:
   - **見つかった場合**: {{storage_read_cmd}} で内容を取得し、テンプレート・ルールを読み込む
   - **見つからない場合**: ユーザーにエラーを通知する。「`ガイドライン/{種別名}` ページがストレージに存在しません。先にガイドラインを作成・保存してください。」
3. **案件コンテキストの取得**: 対象案件のコンテキスト（顧客ページ + 案件ページ）を読み込む
4. **ガイドラインに基づく出力生成**: ガイドライン内のテンプレート・指示に従い、案件コンテキストの情報を埋め込んで出力を生成
5. **不足情報の特定**: ガイドラインが要求する情報で、コンテキストに存在しない項目は ⚠️ 付きで明示し、ユーザーに確認・補完を求める
6. **出力の確認**: 生成結果をユーザーに表示し、修正・承認を確認する
{{^excluded_admin_members}}
7. **レビュアー提案**: チームメンバー設定が存在する場合、ドキュメント種別に応じた適任レビュアーを `💡` 提案する。「チームメンバー候補提案ロジック」に従い、種別→役割マッピング（提案書→営業+PM、見積→営業+PM、技術文書→SE）でマッチング
{{/excluded_admin_members}}

### 対応コマンド

| コマンド | ガイドラインページ | 用途 |
|---------|-------------------|------|
{{#doc_commands}}
| `/{{product_prefix}}-{{command_group}} {{command_action}}` | `ガイドライン/{{guideline_name}}` | {{command_description}} |
{{/doc_commands}}

---

## セッション中の判断フロー

1. {{product_name}}関連の話題 → このプラグインの管轄
2. 顧客案件の話 → `/{{product_prefix}}-deal-load [顧客名]` でコンテキスト復元
3. 機能・規約・手順の質問 → {{product_prefix}}-knowledgeスキルで自動検索して回答
4. 新しいナレッジが生まれた → `/{{product_prefix}}-knowledge-save [カテゴリ/トピック]` で保存を提案
5. PDF等のファイルが提供された → 内容を分析してMarkdown化・適切なページに保存
6. 商談準備が必要 → `/{{product_prefix}}-doc prep [顧客名/案件名]`
7. 提案書・見積書等が必要 → `/{{product_prefix}}-doc proposal`, `/{{product_prefix}}-doc estimate` 等でガイドライン参照生成
8. 要件定義・設計時 → `/{{product_prefix}}-engdoc hearing`, `/{{product_prefix}}-engdoc config`, `/{{product_prefix}}-engdoc testcases`
9. セッション終了 → `/{{product_prefix}}-deal-save [顧客名]` でコンテキスト保存
10. 1日の終わりに → `/{{product_prefix}}-log daily` で活動ログを記録
11. 週末に → `/{{product_prefix}}-log weekly` で週次レポートを生成（ティア分類で情報優先順位付け）
12. 報告資料が必要 → `/{{product_prefix}}-log report monthly` / `/{{product_prefix}}-log report qbr` / `/{{product_prefix}}-log report annual` でデータ集約
13. 定期メンテナンス → `/{{product_prefix}}-admin stale` で鮮度チェック
14. 設定管理 → `/{{product_prefix}}-admin slack`, `/{{product_prefix}}-admin backlog`, `/{{product_prefix}}-admin competitors`, `/{{product_prefix}}-admin pricing`, `/{{product_prefix}}-admin members` で設定変更
15. フォーマット更新時 → `/{{product_prefix}}-admin migrate` でストレージデータを移行
16. タスクがあれば → Todoistへの追加も提案

## 詳細リファレンス

- **`references/context-format.md`** — コンテキスト（顧客ページ・案件ページ）のフォーマット定義、マージルール、未収集情報フラグ
- **`references/index-format.md`** — Home + {{index_count}}つの個別INDEXページのフォーマット定義、更新ルール、再構築手順
- **`references/daily-log-format.md`** — 日次活動ログのフォーマット定義とデータ収集手順（Calendar・Drive・Gmail・Backlog統合）
- **`references/weekly-report-format.md`** — 週次レポートのフォーマット定義、重要度ティア分類ルール、集約手順
- **`references/similarity-check.md`** — 類似エントリ検出プロトコル（階層対応・表記揺れ防止）
- **`references/slack-channels-format.md`** — Slackチャンネル設定のフォーマットとデフォルト値
- **`references/backlog-projects-format.md`** — Backlogプロジェクト設定のフォーマットとデフォルト値
- **`references/kpi-format.md`** — KPI管理ページのフォーマット定義（売上・商談・粗利率等の目標値と実績値の年度単位管理）
- **`references/okr-format.md`** — OKR管理ページのフォーマット定義（年間OKRの目標・KR・進捗・月次アクション管理）
- **`references/competitors-format.md`** — 競合情報設定のフォーマットとデフォルト値
- **`references/pricing-format.md`** — 料金体系設定のフォーマットとデフォルト値
- **`references/team-members-format.md`** — チームメンバー設定のフォーマットとフィールド仕様
