---
description: ストレージのセットアップ状態を検証する
---

ストレージのセットアップ状態を検証する。Skillツールで `{{plugin_name}}:{{product_prefix}}-deal` スキルを呼び出し、「セットアップ検証」セクションの手順に従って実行すること。

以下を検証する:
1. ストレージへの接続確認
2. 必要なディレクトリ/ページ構造の存在確認
3. INDEXページの整合性確認
4. 不足があれば初期構造の作成を提案
